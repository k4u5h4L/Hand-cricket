import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.handcricket.app',
  appName: 'Hand Cricket',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
